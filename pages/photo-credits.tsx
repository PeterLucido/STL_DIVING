import SiteLayout from '../components/SiteLayout';
import PhotoCreditsContent from '../components/page-content/PhotoCreditsContent';

export default function PhotoCreditsPage() {
  return (
    <SiteLayout title={"Photo Credits | STL Diving"}>
      <PhotoCreditsContent />
    </SiteLayout>
  );
}
